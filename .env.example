# Copy this file to .env to override defaults.

# Port the server listens on. Falls back to 5544 if unset.
PORT=5544

# Interface to bind to. 0.0.0.0 makes it reachable on your LAN.
HOST=0.0.0.0

# Where the SQLite database file is stored.
DATABASE_PATH=data/cafforize.db
